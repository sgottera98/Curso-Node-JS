
    <h1>Olá Handlebars</h1>
    {{#if approved}}
        <p>Seu perfil foi aprovado</p>
    {{else}}
        <p>Seu perfil necessita de aprovação para total acesso</p>
    {{/if}}
    <p>O nome do usuário é {{user.name}} {{user.surname}}, e ele tem {{user.age}} anos</p>
    {{#if auth}}
        <p>
            Você está autenticado, <a href="/dashboard">clique aqui</a> para acessar o dashboard
        </p>
    {{/if}}